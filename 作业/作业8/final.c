#include <stdio.h>
#include <math.h>
#include <stdlib.h>
#define MAX_SIZE 3000
#define MAX(a,b) ((a)>(b)?(a):(b))
int main()
{
    int N1,N2,i,max=0,flag=0,c1=0,c2=0,num=0;
    int num1[MAX_SIZE]={0};
    while(scanf("%d",&N1)!=EOF)
    {

          for(i=0;i<N1;i++)
        {

            scanf("%d",&num1[i]);
        }







    }
      return 0;
}
