#include <stdio.h>
#include <string.h>
#define maxsize 300
int main()
{
    unsigned int num,i,weishu=0,temp=0;
    int k;
    scanf("%u",&num);
    scanf("%d",&k);
    while(1)
    {
        temp=num/10;
        weishu++;
        if(temp==0) break;
    }
    if()










    return 0;
}
