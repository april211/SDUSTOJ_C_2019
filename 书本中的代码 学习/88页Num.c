#include <stdio.h>
#include <string.h>
    void main()
{
    char InStr[80];
    unsigned int i;
    int NumOfData=0;
    int NumOfChar=0;
    int NumOfOther=0;
    printf("input a string:");
    gets(Instr);
    for (i=0;i<strlen(Instr);i++)
    {
        if (Instr[i]>=)
    }
}
